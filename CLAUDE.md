# CLAUDE.md

## Regras de código

- NUNCA adicione comentários no código (nem `//`, nem `/* */`, nem `#` em YAML/.env). O código deve ser autoexplicativo por meio de nomes claros.
- Ao editar um arquivo que tenha comentários, remova-os.
